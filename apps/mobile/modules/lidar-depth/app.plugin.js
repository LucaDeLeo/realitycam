/**
 * Expo config plugin for LiDAR depth module
 *
 * This module uses the Expo Modules API and doesn't require
 * additional native configuration beyond what expo-module.config.json provides.
 */
module.exports = (config) => {
  return config;
};
